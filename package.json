{
  "name": "mt0token",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "deploy": "truffle migrate --reset --network bsctest",
    "deployspec" : "truffle migrate --reset -f 3 to 3 --network bsctest",
    "verify0": "truffle run verify Mt0Token --network bsctest",
    "verify1": "truffle run verify Mt1Token --network bsctest",
    "verify_staking": "truffle run verify Staking --network bsctest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2",
    "@truffle/hdwallet-provider": "^1.5.1",
    "truffle-plugin-verify": "^0.5.15"
  }
}
